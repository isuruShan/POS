<%= form_for(pos_usr_lorry) do |f| %>
  <% if pos_usr_lorry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pos_usr_lorry.errors.count, "error") %> prohibited this pos_usr_lorry from being saved:</h2>

      <ul>
      <% pos_usr_lorry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reg_number %>
    <%= f.number_field :reg_number %>
  </div>

  <div class="field">
    <%= f.label :number %>
    <%= f.text_field :number %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>
  <div class="field">
    <%= f.label :pos_usr_lorry_category_id %>
    <%= f.collection_select :pos_usr_lorry_category_id , PosUsrLorryCategory.order(:category), :id, :category, include_blank: true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
